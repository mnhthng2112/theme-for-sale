<h1>{{ product.title }}</h1>
<div> {{ product.price | money }} </div>
<div> {{ product.description }} </div>

{% form 'product', product: product %}
    <select name="id">
        {}
    </select>
{% endform %}